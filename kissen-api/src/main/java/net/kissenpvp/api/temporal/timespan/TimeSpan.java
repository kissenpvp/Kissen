package net.kissenpvp.api.temporal.timespan;

/**
 * Represents a temporal abstraction that can define a span of time.
 * This serves as a base interface for more specific time span implementations.
 * <p>
 * The {@code TimeSpan} interface provides a common structure for distinguishing between various
 * types of time spans, whether defined with specific durations and periods or open-ended.
 * <p>
 * Classes implementing this interface are used in scenarios requiring precise
 * representations and operations involving spans of time.
 *
 * @author Ivo Quiring
 */
public interface TimeSpan {}
